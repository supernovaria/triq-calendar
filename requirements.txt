requests==2.33.1
beautifulsoup4==4.14.3
# Pinned: code targets the ics 0.7.x API (ics.grammar.parse.ContentLine,
# Event.duration, writelines). The 1.x rewrite removes these and will break.
ics==0.7.3
